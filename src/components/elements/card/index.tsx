import React from "react";

import { StyledCard } from "./styles";

export interface CardProps {
    asset: {};
}

const Card = ({ }: CardProps) => {
    return (
        <StyledCard>
            
        </StyledCard>
    );
};

export default Card;
